#include <stdio.h>

int main()
{
    // Area of a Square
    /* float side;
    printf("Enter the side of the square: ");
    scanf("%f", &side);

    printf("Area of the square: %.2f", side * side); */

    // Area of a Rectangle
    /* float length, width;
    printf("Enter the length of the rectangle: ");
    scanf("%f", &length);
    printf("Enter the width of the rectangle: ");
    scanf("%f", &width);

    printf("Area of the rectangle: %.2f", length * width); */

    // Area of a Circle
    /* float PI = 3.14;
    float radius;
    printf("Enter the radius of the circle: ");
    scanf("%f", &radius);

    printf("Area of the circle: %.2f", PI * radius * radius); */

    return 0;
}